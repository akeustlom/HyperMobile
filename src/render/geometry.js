export function buildStickerGeometry(piece, sticker) {
    // TODO: account for piece explode, facet spacing, and sticker shrink
    // get sticker home position
    // take sticker through piece transform
    // stop at world space; projection and view angle are later steps
    // create an (N-1)cube centered on the sticker
    // create an edge table
    // return vertices, edges, color
}